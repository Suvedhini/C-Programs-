#include<iostream>
using namespace std;
int main(){
    int n,sum=0;
    cout<<"Enter the number: ";
    cin>>n;
    for (int i = 0; i < 10; i++)
    {
        sum=sum+n;
    }
    cout<<"Sum is: "<<sum;
    return 0;
}
